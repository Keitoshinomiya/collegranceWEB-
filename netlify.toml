# Netlify Configuration File
# COLLEGRANCE Website Deployment Settings

[build]
  # 静的HTMLサイト + Netlify Functions
  command = "npm install"
  publish = "."
  functions = "netlify/functions"

  [build.environment]
    NODE_VERSION = "18"

[functions]
  included_files = ["catalog_full.json", "products.json"]

# Netlify Functions: /api/* → /.netlify/functions/*
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
  force = true

# ────────────────────────────────────────────────────
# Scheduled Functions
# ────────────────────────────────────────────────────
# ★ テスト用スケジュール: 毎日 JST 12:00 (= UTC 03:00)
#   → 今日のテスト送信に使用。BROADCAST_ENABLED=true にして実行。
#
# ★ 本番スケジュール（3/29 JST 00:00）に切り替える場合:
#   schedule = "0 15 28 3 *"
#   （2025-03-29 JST 00:00 = UTC 2025-03-28 15:00）
#
# ★★ 安全スイッチ ★★
# Netlify 環境変数 BROADCAST_ENABLED=true にしないと
# スケジュールが起動しても実際の LINE 送信はスキップされます。
# LINE_CHANNEL_ACCESS_TOKEN も必ず設定してください。
[functions."scheduled-broadcast"]
  schedule = "0 3 * * *"

# ブランチごとのデプロイ設定
[context.production]
  publish = "."
  
[context.deploy-preview]
  publish = "."

# HTTPヘッダーの設定（セキュリティとパフォーマンス）
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-XSS-Protection = "1; mode=block"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
    
# CSS・JSファイルのキャッシュ設定
[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# HTMLファイルのキャッシュ設定
[[headers]]
  for = "/*.html"
  [headers.values]
    Cache-Control = "public, max-age=0, must-revalidate"

# 画像ファイルのキャッシュ設定
[[headers]]
  for = "/assets/images/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# リダイレクト設定
[[redirects]]
  from = "/tokushoho"
  to = "/tokushoho.html"
  status = 200
  force = false

# 404エラーページの設定（存在する場合）
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 404

# フォーム送信の設定（将来的なお問い合わせフォーム用）
[build.processing]
  skip_processing = false

[build.processing.css]
  bundle = true
  minify = true

[build.processing.js]
  bundle = true
  minify = true

[build.processing.html]
  pretty_urls = true

[build.processing.images]
  compress = true
